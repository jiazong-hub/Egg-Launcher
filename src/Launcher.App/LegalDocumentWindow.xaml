<Window x:Class="Launcher.App.LegalDocumentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="Assets/egg-mark.png"
        Width="760"
        Height="680"
        MinWidth="540"
        MinHeight="420"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock x:Name="HeadingText" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Margin="0,6,0,0" Foreground="{DynamicResource SecondaryBrush}"
                       Text="{DynamicResource OfflineDocumentHint}" />
        </StackPanel>

        <TextBox x:Name="DocumentTextBox" Grid.Row="1" Margin="0,20,0,18"
                 Padding="18" IsReadOnly="True" AcceptsReturn="True"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled" FontSize="14"
                 TextBlock.LineHeight="23" />

        <WrapPanel Grid.Row="2" HorizontalAlignment="Right">
            <Button Padding="16,9" Content="{DynamicResource CopyAll}" Click="CopyDocumentButton_Click" />
            <Button Margin="0" Padding="20,9" Content="{DynamicResource Close}" IsDefault="True" IsCancel="True" />
        </WrapPanel>
    </Grid>
</Window>
