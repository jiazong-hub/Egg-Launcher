<Window x:Class="Launcher.App.LocalServiceCloseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource CloseEggTitle}"
        Width="560"
        Height="330"
        MinWidth="460"
        MinHeight="280"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="21" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}"
                   Text="{DynamicResource LocalServiceRunning}" />
        <TextBlock Grid.Row="1" Margin="0,12,0,24" TextWrapping="Wrap"
                   Foreground="{DynamicResource SecondaryBrush}"
                   Text="{DynamicResource LocalServiceCloseBody}" />

        <WrapPanel Grid.Row="2" HorizontalAlignment="Right">
            <Button Padding="16,9" Content="{DynamicResource Cancel}" IsCancel="True" Click="CancelButton_Click" />
            <Button Padding="16,9" Content="{DynamicResource CloseWindowOnly}" IsDefault="True" Click="KeepServiceButton_Click" />
            <Button Margin="0" Padding="16,9" Content="{DynamicResource StopServiceAndExit}" Click="StopServiceButton_Click" />
        </WrapPanel>
    </Grid>
</Window>
